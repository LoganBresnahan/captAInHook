# Dev-only build tooling — never committed (ADR-0008: Node is dev-only).
node_modules/
.vite/

# Build output goes to ../ui/ (committed there), never here.
dist/

# tsc incremental state (build artifact)
tsconfig.tsbuildinfo

# Playwright E2E run artifacts (traces, videos, reports — regenerated per run)
test-results/
playwright-report/

