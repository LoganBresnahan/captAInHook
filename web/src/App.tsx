// Placeholder shell (web-scaffold slice). Real screens — Live trace,
// Supervision, Policy, Harnesses, Status — arrive as islands in later slices
// (ADR-0008 d1/d8), each reading its slice of the one Zustand store.
export function App() {
  return (
    <main>
      <h1>captAInHook</h1>
      <p>Management GUI — scaffold. Screens land in later slices.</p>
    </main>
  );
}
